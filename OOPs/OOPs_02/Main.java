package OOPs.OOPs_02;

public class Main {
    public static void main(String[] args) {
        /*
        Human tejas = new Human("Tejas", 21, false);
        Human rahul = new Human("Rahul", 20, true);
        Human ram = new Human("Ram", 28, true);

        tejas.displayInfo();
        rahul.displayInfo();
        ram.displayInfo();

        System.out.println(Human.population);
        // to print or access or modify static variables, good practise is 'className.staticVariable'
        */
    }

    /*
    // Declaring a static function which are object independent, which means only class can access it not the object itself
    static void fun1() {
        // funny();
            // Non-static method 'funny()' cannot be accessed from a static context
            // to access this function, do the following method
            // We have a class named Main (line no. 3), we can create an object to access non-static or instance function
                // Main obj = new Main();
                // obj.funny();
    }

    // Declaring an instance function
    void fun() {
        funny();
            // Non-static method 'funny()' can be accessed from a non-static context only
    }

    // Declaring another instance function
    void funny() {
        // fun1();
            // Static method 'fun()' can be accessed from a non-static context
    }
    */
}
